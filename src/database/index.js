const mysql = require('mysql2/promise');

const connectToMySql = async () => {

    const config = {
        host: "localhost",
        user: "root",
        password: "senha_root",
        database: "tech_academy"
    }



}